org.gradle.jvmargs=-Xmx3G
org.gradle.parallel=true

# Matched to the user's Prism instance "Skyblock 1.26.1.2".
# No yarn mappings exist for 26.1, so the build uses Mojang official mappings.
minecraft_version=26.1.2
loader_version=0.19.3
fabric_version=0.155.2+26.1.2

# The config library behind the NEU-style GUI. Same artifact crittermod ships.
moulconfig_version=4.7.2
# Matches fabric-language-kotlin 1.13.13+kotlin.2.4.10 in the user's instance.
kotlin_version=2.4.10
# Supplies the Kotlin runtime MoulConfig needs. Matches the user's instance.
fabric_kotlin_version=1.13.13+kotlin.2.4.10
# Matches the modmenu-18.0.0 in the user's instance.
modmenu_version=18.0.0

mod_version=0.3.5
maven_group=dev.squidutils
archives_base_name=squidutils
